# 财务团队共享知识库 · 设计思路文档

> 本文档是本 Demo 的配套讲稿。覆盖产品定位、信息架构、技术选型、关键设计决策、扩展性、安全权限、演进路线 8 个章节。
> 阅读时间：8-12 分钟。

---

## 一、设计原则与定位

**问题陈述**：初创企业的财务团队，最怕三件事——

1. **知识在人脑里，不在系统里**：老员工离职，业务断档；新员工上手慢，反复问同样的问题。
2. **每月写报告耗尽 leader 周末**：从拉数据、对账、写分析、做 PPT，一份月报花掉 8-12 小时。
3. **流程跑在 Excel 和微信里**：合规、税务、审计随时可能踩坑，没有可追溯的沉淀。

**产品定位**：一个**集团队第二大脑、自动化中枢、共享能力空间于一体**的 Web 平台。

**三层价值**：

| 层 | 价值 | 对应模块 |
|---|---|---|
| **沉淀层** | 把所有流程、知识、模板、项目档案结构化沉淀 | 01-04 |
| **数据层** | 把所有财务事实集中、可追溯、可被 AI 引用 | 05 |
| **自动化层** | 把重复劳动（报告、校验、对账）封装为 Skills，团队共建共用 | 06-07 |

**对标参考**：飞书 Wiki（协作体验）+ Notion（信息架构）+ Andrej Karpathy LLM Wiki（LLM 友好结构）+ Anthropic Skills（能力贡献模式）。

---

## 二、信息架构（八大模块为什么这么分）

按"**日常用 → 沉淀 → 自动化**"三层逻辑，把财务工作的所有触点拆成 8 个一级模块：

```
日常用：00 主页 / 01 流程 SOP / 03 模板库
沉淀：  02 知识沉淀 / 04 项目档案
事实：  05 数据中心
自动化：06 报告中心 / 07 Skills 工坊
```

### 各模块的边界与示例
- **00 团队主页**：组织、OKR、当周关注。新成员 30 秒上手。
- **01 流程 SOP**：报销、月结、年审等可执行步骤。每个 SOP 含步骤、责任人、SLA、异常处理。
- **02 知识沉淀**：税务、会计政策、合规、行业知识、踩坑日志。**LLM 问答的主战场**。
- **03 模板库**：合同、报表、单据、邮件话术。
- **04 项目档案**：每轮融资、每次审计、每次系统上线的完整档案。
- **05 数据中心**：所有 KPI、预算实际、现金流。**所有报告和 LLM 回答的事实层**。
- **06 报告中心**：周报 / 月报 / 季年报，自动生成 + 推送。
- **07 Skills 工坊**：把重复任务封装成可复用的 skill。

### 为什么这么分
- **避免一锅炖**：流程 vs 知识 vs 模板 vs 数据天然不同，强行混在一起，搜索和归档都会乱。
- **匹配工作模式**：财务工作 80% 时间在执行流程（01/03）和处理事务（05），20% 在沉淀和复盘（02/04），剩下的（06/07）是杠杆。八大模块和这个时间分布契合。
- **方便授权**：项目档案和数据中心常含敏感信息，单独成模块便于做权限隔离。

---

## 三、Karpathy LLM Wiki 思路落地

**Karpathy 的核心观点**：传统 Wiki 是为人写的（章节、超链接），LLM Wiki 是为 LLM 写的——结构必须让模型能高效消化。

**落地到本系统的 6 条具体规则**：

### 1. 每个文档单一主题，明确边界
不要把"报销 + 付款"塞一篇。LLM 检索时拿到的 chunk 才是"高密度"的，不会被干扰。

### 2. 统一 frontmatter（YAML 头）
每篇文档强制带：
```yaml
---
title: 月结关账流程
owner: 王芳
updated: 2026-04-30
version: 2.1
tags: [SOP, 月结, 财务部]
related: [报销流程, 银行对账, 管理报表]
---
```
**好处**：LLM 可以根据 tags / updated 做精准筛选（"过去 30 天更新的税务文档"）。Demo 里每篇文档顶部都展示了这个 frontmatter（见知识沉淀中任意文档）。

### 3. 结构化 Markdown 模板
不同类型文档有固定骨架：
- **SOP**：背景 / 步骤表 / 责任人 / SLA / 异常处理 / 常见误区
- **知识**：定义 / 政策依据 / 实务处理 / 常见误区 / 历史案例
- **项目**：概览 / 时间线 / 关键文件 / 财务工作要点 / 复盘

骨架统一让 LLM 能按位置抽取信息，无需"理解整篇"。

### 4. 交叉引用（Wiki Link）
用 `[[文档 ID]]` 语法。Demo 里 SAFE 文档引用了 A 轮项目档案，AI 问答也会跨文档关联（参见预置问答"我们 A 轮 SAFE 的转股条件是什么？"——同时引用了项目档案和 SAFE 会计处理）。

### 5. 版本化 + Diff
每次编辑保留快照。LLM 可以回答"这个流程半年前是怎样的"。

### 6. 分块向量化 + RAG
- 切片：按 H2 章节，每片 ~500 token，重叠 50 token
- 模型：voyage-3 / bge-m3
- 索引：pgvector
- 检索：top-K（K=5），并把 frontmatter 作为元数据过滤

> 详见 `integrations.js` 中的 `LLM.chat` / `LLM.embed`。

---

## 四、Skills 工坊（团队共享能力空间）

### 定位
> "每个人解决过一次的问题，都能沉淀成可复用工具，全团队调用。"

参考 **Anthropic Skills** + **superpowers-zh**（用户 home 已部署）模式，把财务工作中的高频重复任务（写月报、校验发票、对账、合同审阅）封装为团队共享的 Skill。

### Skill 结构
```
skills/
└── monthly-report/
    ├── SKILL.md         # 元数据 + 执行步骤
    ├── templates/       # 报告模板
    └── examples/        # 历史输出示例
```

### SKILL.md frontmatter
```yaml
---
name: monthly-report
description: 生成月度经营管理报告
contributor: CFO
domain: [报告生成, 月结]
inputs: [当月财务数据, 上月对比, 预算]
outputs: [HTML 报告, 飞书卡片]
status: stable     # draft / review / stable / deprecated
version: 1.2
---
```

### 贡献流程（5 步）
1. **提案**：在 Skills 工坊点"在线生成 Skill"，填 4 字段（场景 / 输入 / 输出 / 触发）
2. **起草**：LLM 自动起草 SKILL.md 初稿
3. **评审**：贡献者精修 → 提交评审 → CFO + 1 名同事 review
4. **上线**：合并到 Skills registry，团队成员可在工坊调用
5. **统计**：记录调用次数、用户反馈，定期淘汰低用率 skill

### Demo 里的 6 个示例 skill
| Skill | 用途 | 调用次数 |
|---|---|---|
| `monthly-report` | 月报生成 | 142 |
| `weekly-cash-digest` | 周资金日报 | 67 |
| `vat-checker` | 进项发票合规校验 | 89 |
| `reconcile-bank` | 银行对账自动化 | 312 |
| `contract-finance-review` | 合同财务条款审阅 | 28 |
| `invoice-validator` | 发票真伪校验 | 215 |

> 在 demo 里点任意 skill 卡片可看完整 SKILL.md。

---

## 五、自动化中枢（报告 / 推送链路）

把"leader 周末写报告"压成"喝咖啡的时间"，靠一条标准化链路：

```
[定时触发器]                       cron: T+5 09:00
    ↓
[DataSources.fetchKPIs]           从 ERP / 多维表格拉数
    ↓
[Sanitizer.redact]                脱敏（去 PII / 银行账号）
    ↓
[LLM.runSkill('monthly-report')]  套模板 + 生成经营分析
    ↓
[人工复核]                         CFO 在网页上 review，可改可批
    ↓
[Feishu.pushMessage]              推送到"财务管理层"群
    ↓
[Audit.log]                       全程留痕
```

### Demo 里的对应交互
- 进入 **06 报告中心** → 点"⚡ 一键生成月报"
- 看 4 阶段进度（拉数 / 算环比 / LLM 写分析 / 套模板）
- 出完整月报（KPI 表 + 5 个章节）
- 点"💬 推送到飞书" → toast 通知

### 关键设计
- **关键数字必须从 Data Hub 取**，禁止 LLM 编造（防幻觉）
- **经营分析段落由 LLM 写，但必须人审**（防错误结论流出）
- **失败重试 + 降级**：LLM 失败时退化为模板填空版本（不影响业务）

---

## 六、接入扩展性（飞书 / 钉钉 / LLM / ERP）

**所有外部系统接入都集中在 `integrations.js`**——一个文件、所有桩函数、注释清晰。

### 4 类 Connector
| 类别 | 当前桩函数 | 真实接入说明 |
|---|---|---|
| **飞书** | `Feishu.syncWiki / pushMessage / sso / fetchBitable` | 飞书开放平台，需 app_id + app_secret，文档完整 |
| **钉钉** | `Dingtalk.pushMessage / submitForm` | 备选 IM，多 IM 共存场景 |
| **LLM** | `LLM.chat / embed / runSkill / draftSkill` | 走统一 LLM 网关，便于切模型 / 加缓存 / 审计 |
| **数据源** | `DataSources.fetchKPIs / fetchBankFlow / fetchKingdee` | ERP（金蝶）+ 银行接口 + 数据仓库 |

### 切换厂商成本
- 飞书 → 钉钉：只需把所有 `Feishu.xxx` 调用替换为 `Dingtalk.xxx`，函数签名一致。
- Claude → 国内大模型（通义 / 文心 / Kimi / DeepSeek）：只需在 `LLM.chat` 内换 endpoint。
- 金蝶 → 用友：在 `DataSources.fetchKingdee` 内换 SDK。

### 前端可移植性
- 当前 demo 是单 HTML + Tailwind CDN，任何浏览器双击即开。
- 真实部署时（M5 阶段）可迁移到 Next.js + TipTap，**所有 mock 数据替换为 API 调用即可**——`mockData` 这个全局对象的所有字段，都已经在 `integrations.js` 里有对应的 fetch 函数桩。

---

## 七、数据安全与权限

财务数据高敏，安全是底线，不是加分项。

### 权限分层（RBAC）
| 角色 | 访问范围 |
|---|---|
| 超管 / CFO | 全部 |
| 财务全员 | 除"薪酬"、"未公开融资"外全部 |
| 业务部门 | 流程 SOP + 模板库 + 部分知识沉淀 |
| 外部审计师 | 项目档案某子目录 + 限期访问 |

### 敏感模块单独权限组
- 薪酬数据（HR + CFO + 必要财务人员）
- 税务争议、稽查应对
- 未公开融资进展
- 关联交易底稿

### LLM 调用安全
- **脱敏中间层**：`Sanitizer.redact()` 在文本送 LLM 前，替换银行账号、身份证、手机号、敏感金额。
- **企业版 API**：调用 Claude 走企业账号，开 zero data retention，不参与训练。
- **敏感问题阻断**：检测到询问"某员工薪酬"等敏感问题时，直接拒答而非 LLM 处理。

### 审计日志
- `Audit.log()` 记录：谁查了什么、谁改了什么、LLM 回答过什么。
- 落库到独立审计库（append-only），与业务库分离。
- 半年定期合规复盘。

### 数据可移植性
- 所有文档 = Markdown + JSON metadata，**任何时候可一键导出**。
- 不锁定平台。即使飞书涨价或服务问题，也能切到 Notion / 自建。

---

## 八、演进路线

| 阶段 | 时长 | 关键交付 | 目的 |
|---|---|---|---|
| **M1 骨架** | 2 周 | 八大模块目录 + frontmatter 规范 + 文档模板（SOP / 知识 / 项目）+ 飞书 Wiki 落地 | 团队开始按规范写 |
| **M2 RAG 问答** | +1 月 | 飞书 → Git 同步管道 + pgvector 向量库 + RAG 问答机器人 | LLM 能基于知识库回答 |
| **M3 自动报告** | +1 月 | 周报 / 月报 skill + 数据中心 + 飞书定时推送 | 报告自动化 |
| **M4 Skills 工坊** | +1 月 | Skills 注册中心 + 在线生成 + 评审流程 + 调用统计 | 团队共建 |
| **M5 自建 Web** | 按需 | Next.js + TipTap 自建前端 + 嵌入式 LLM 问答 + 完整 RBAC | 深度定制（可选） |

### 第一周该做什么（最务实的回答）
1. **Day 1-2**：建飞书 Wiki 八大模块 + 写第一篇 SOP（报销流程，模范级标杆）
2. **Day 3-4**：定 frontmatter 模板 + 三类文档模板，开 1 小时全员培训
3. **Day 5**：每人认领 1-2 篇文档，下周交付

不要等系统完美再用。**先让团队按规范写起来，3 个月后再上 LLM**。

---

## 九、不做什么（避免 scope creep）

| 不做 | 理由 |
|---|---|
| 自研编辑器 | 飞书 Wiki 已经够用，造轮子浪费 |
| 实时协作（co-editing） | 飞书 / Notion 已有，靠这个吸引不到人 |
| 通用 OA（流程审批、考勤） | 不是财务团队第一痛点，让飞书 / 钉钉做 |
| 移动端原生 App | 飞书 / 钉钉移动端已能看 Wiki，不重复 |
| 跨企业 SaaS 化 | 这是内部工具，不是创业项目 |

---

## 十、附：Demo 演示动线（5 分钟讲完）

1. **打开 index.html** → 首屏（30 秒）："这是团队第二大脑"
2. **点击 02 知识沉淀 → SAFE 协议** → 展示 frontmatter（30 秒）：解释 Karpathy LLM Wiki 思路
3. **右栏 AI 助手 → 点"我们 A 轮 SAFE 的转股条件是什么？"** （45 秒）：展示 RAG + 引用源
4. **点击 06 报告中心 → 一键生成月报** → 看进度 → 看完整报告 → 推送飞书（90 秒）：展示自动化中枢
5. **点击 07 Skills 工坊 → 浏览 6 个 skill → 点"在线生成 Skill"** （60 秒）：展示团队贡献模式
6. **打开 integrations.js**（30 秒）：展示接入层设计，"接真实 API 只要填这几个函数"
7. **回到首屏**（15 秒）：总结演进路线、第一周做什么

总计 5 分钟，覆盖产品定位、信息架构、AI 能力、自动化、Skills、扩展性 6 个维度。
